#include <iostream>
#include <vector>
#include <algorithm>

#define all(a) a.begin(), a.end()
#define sz(a) a.size()
#define pb push_back

using namespace std;
using ll = long long;

const ll MOD = 1e9 + 7;
const char nl = '\n';
const int maxN = -1;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << 1;
}